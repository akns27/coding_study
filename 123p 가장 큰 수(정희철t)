#include <stdio.h>

main() {
  int a, i, m1=0, m2=0;
  for(i=0;i<10;i++)
    {
      scanf("%d", &a);
      if(a%2==1)
        if(m1<a)
          m1=a;
      if(a%2==0)
        if(m2<a)
          m2=a;
    }
  if(m1>0)
    printf("%d ", m1);
  if(m2>0)
    printf("%d ", m2);
}

/*프로그램은 main 함수로 시작합니다.

변수 선언:

a: 입력받은 정수를 저장하는 변수
i: 반복문에서 사용하는 변수
m1, m2: 가장 큰 홀수와 가장 큰 짝수를 저장하는 변수로, 초기값은 0으로 설정됩니다.
반복문: for 문을 사용하여 10번 반복합니다.

scanf 함수를 사용하여 사용자로부터 정수를 입력받습니다.
입력된 정수 a를 검사하여 홀수인지 짝수인지 확인합니다.
홀수와 짝수 검사:

if (a % 2 == 1)을 사용하여 a가 홀수인지 확인합니다.
홀수인 경우, m1과 비교하여 m1보다 큰 값이면 m1에 해당 값을 저장합니다.
if (a % 2 == 0)을 사용하여 a가 짝수인지 확인합니다.
짝수인 경우, m2와 비교하여 m2보다 큰 값이면 m2에 해당 값을 저장합니다.
결과 출력:

반복문이 끝나면 가장 큰 홀수와 가장 큰 짝수를 출력합니다.
if (m1 > 0)을 사용하여 m1이 0보다 큰지 확인하고, 큰 경우 m1 값을 출력합니다.
if (m2 > 0)을 사용하여 m2가 0보다 큰지 확인하고, 큰 경우 m2 값을 출력합니다.*/
