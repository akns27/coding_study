#include <stdio.h>

int f(int n) {
    if(n==1)
        return 1;
    else
        return f(n-1) * n;//*을 +로 하면 1~n 까지의 합, 
    
}

int main()
{
    int n;
    scanf("%d", &n);
    printf("%d\n", f(n));
}
