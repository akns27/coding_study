#include <stdio.h>
#include <string.h>

int main() {
  char str[101];
  int i;
    gets(str);
    for(i=strlen(str)-1; i>=0; i--)
      printf("%c", str[i]);
}

/*라이브러리 포함:

#include <stdio.h>: 표준 입출력 함수를 사용하기 위해 stdio.h 헤더 파일을 포함합니다.
#include <string.h>: 문자열 처리 함수를 사용하기 위해 string.h 헤더 파일을 포함합니다.
main 함수:

int main(): 프로그램의 진입점입니다. main 함수는 int형을 반환하므로 int으로 선언됩니다.
변수 선언:

char str[101]: 크기가 101인 문자열을 저장하기 위한 문자 배열 str을 선언합니다.
int i: 반복문에서 사용할 변수 i를 선언합니다.
문자열 입력:

gets(str): 사용자로부터 문자열을 입력받아 str 배열에 저장합니다. (주의: gets 함수는 버퍼 오버플로우의 위험이 있으므로 추천되지 않는 함수입니다.)
문자열 역순 출력:

for 반복문을 사용하여 str의 마지막 문자부터 처음 문자까지 반복합니다. (strlen(str)-1은 str의 마지막 인덱스를 의미합니다.)
printf("%c", str[i]): str 배열의 i번째 인덱스에 해당하는 문자를 출력합니다. 따라서 입력된 문자열을 역순으로 출력합니다.
___________________
strlen(str)-1은 문자열의 길이에서 1을 뺀 값입니다.

strlen() 함수는 문자열의 길이를 반환하는 함수(=문자열 길이)이며, 널 종료 문자 ('\0')를 제외한 문자의 수를 반환합니다.

따라서 strlen(str)-1은 문자열의 마지막 인덱스를 의미합니다. 문자열은 0부터 시작하는 인덱스를 가지므로, 마지막 인덱스는 문자열의 길이에서 1을 뺀 값입니다.

예를 들어, 문자열 str의 길이가 5라면 strlen(str)은 5를 반환하고, strlen(str)-1은 4를 반환합니다. 이는 배열 인덱스와 맞물려 문자열의 마지막 문자를 가리킵니다.*/
